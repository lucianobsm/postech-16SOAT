# ==========================================================================
# ATENCAO: NAO commite segredos REAIS neste arquivo.
# Os valores abaixo sao apenas PLACEHOLDERS para ambiente local/exemplo.
# Em producao, substitua por Sealed Secrets, External Secrets Operator, ou
# crie o Secret via linha de comando (fora do Git). Ex.:
#   kubectl -n tech-challenge create secret generic tech-challenge-secrets \
#     --from-literal=DB_USER=... --from-literal=DB_PASS=... \
#     --from-literal=JWT_SECRET=... --from-literal=MAIL_USERNAME=... \
#     --from-literal=MAIL_PASSWORD=...
# ==========================================================================
apiVersion: v1
kind: Secret
metadata:
  name: tech-challenge-secrets
type: Opaque
stringData:
  DB_USER: postgres
  DB_PASS: postgres
  JWT_SECRET: TM5Qx59S6yrYAGOyT5gi_QFKzexihvBtdAdpctLDqCE
  MAIL_USERNAME: change-me@example.com
  MAIL_PASSWORD: change-me
